# focustime
This app is a simple note taking application (like notepad) but has a time tracking function. All lines are a list of tasks and selecting one line and clicking start will keep track of time spent on that task. It will be logged at the bottom so that it can be copy pasted anywhere else. 
